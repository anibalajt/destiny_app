import AsyncStorage from '@react-native-community/async-storage';
export const getAuthorization = async () => {
  // let response = await AsyncStorage.getItem("authorization");
  // response = JSON.parse(response);
  // return response;
};
